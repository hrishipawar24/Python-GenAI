# Create a list of numbers
number_list = [5, 10, 15, 20]
# Calculate the sum of all elements
list_sum = sum(number_list)
# Print the sum
print(f"The list is: {number_list}")
print(f"The sum of all elements is: {list_sum}")

# *Self-Correction/Alternative Interpretation: If it meant sum of the actual index numbers (0, 1, 2, 3):*
# indices = [0, 1, 2, 3] # The indices of the list elements
# sum_of_indices = sum(indices)
# print(f"The sum of the indices (0, 1, 2, 3) is: {sum_of_indices}")